include ../mixins/services.jade

section#services.services.scrollspy
    .container
        h2: span Services
        h4 We offer a wide range of services from data mining to train custom models
        .row.big-icons-row.service-items.center
            each service in site.data['services']
                +service(service)
